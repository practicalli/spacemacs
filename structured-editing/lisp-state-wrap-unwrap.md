# Wrap & unwrap with a list

You can wrap an element in a list, `( )` using wrap, `w`.

To remove a list from one or more element use unwrap, `W`

> **TODO** Video of wrapping and unwrapping symbols, words and expressions.


